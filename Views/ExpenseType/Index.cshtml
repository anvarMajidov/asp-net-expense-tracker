@model IEnumerable<InAndOut.Models.ExpenseType>

<div class="container p-2">
    <div class="row pt-4 pb-2">
        <div class="col-9">
            <h2 class="text-primary">Expenses</h2>
        </div>
        <div class="col-3 text-right">
            <a asp-action="Create" class="btn btn-info text-white">New expense types</a>
        </div>
    </div>
    <table class="table table-striped table-bordered">
        <tr>
            <th width="70%">Name</th>
            <th width="30%">Actions</th>
        </tr>
        @if (Model.Count() > 0)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td class="d-flex">
                        <form method="post" class="w-75" asp-action="Delete" asp-route-Id="@item.Id">
                            <button class="btn btn-danger w-100" type="submit">Delete</button>
                        </form>
                        <a class="btn btn-info ml-2 w-75" asp-action="Edit" asp-route-Id="@item.Id">Edit</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td>No data found</td>
            </tr>
        }
    </table>
</div>
